<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">
    <persistence-unit name="AgenteMensajesIAPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>com.ia.mensajes.agentemensajesia.model.Usuario</class>
        <class>com.ia.mensajes.agentemensajesia.model.Mensaje</class>
        <properties>
            <!-- Este driver se usará para la conexión con PostgreSQL -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            
            <!-- El dialecto correcto para PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>

            <!-- ======================= CAMBIO CLAVE AQUÍ ======================= -->
            <!-- Cambiamos 'validate' por 'update' para que Hibernate cree las tablas -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- =============================================================== -->
            
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.jdbc.batch_size" value="50"/>

            <!-- Las propiedades de URL, usuario y contraseña se leerán de las
                 variables de entorno en Render, por lo que no es necesario
                 ponerlas aquí. -->

        </properties>
    </persistence-unit>
</persistence>
